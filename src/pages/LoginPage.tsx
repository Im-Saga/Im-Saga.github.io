import { PagePlaceholder } from './PagePlaceholder';
export function LoginPage() { return <PagePlaceholder eyebrow="Welcome back" title="Sign in">Authentication will be added in the next phase.</PagePlaceholder>; }
